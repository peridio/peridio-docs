```
Usage: peridio config profiles create [OPTIONS] --name <NAME>

Options:
      --name <NAME>        The profile's name
      --api-key <API_KEY>  The profile's api_key. Can be omitted to supply the value interactively
      --no-input           Disable interactivity
  -h, --help               Print help

```